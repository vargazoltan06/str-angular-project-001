import { RandomiserPipe } from './randomiser.pipe';

describe('RandomiserPipe', () => {
  it('create an instance', () => {
    const pipe = new RandomiserPipe();
    expect(pipe).toBeTruthy();
  });
});
